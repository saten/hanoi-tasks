<%= form_for(@task_group) do |f| %>
  <% if @task_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_group.errors.count, "error") %> prohibited this task_group from being saved:</h2>

      <ul>
      <% @task_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
